---
status: proposed
---
# Include archived children with `--archived`

`wi children <ref> --archived` currently returns active and archived children together. Without the flag, archived items are omitted. Recursive listings apply the same rule at each level.

The open decision is whether the flag should continue to include both groups together.
